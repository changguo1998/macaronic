#!mac
[contract]
count = "int"
total = "float"
ok = "bool"
msg = "str"

#!shell
count=40
total=2.5
ok=true
msg="hello from shell"

#!python
count: int = count + 1
msg: str = msg + " & python"

#!go
fmt.Printf("final values: count=%d total=%g ok=%t msg=%s\n", count, total, ok, msg)
