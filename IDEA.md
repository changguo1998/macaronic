# Macaronic — 项目核心思想

## 是什么

一个类似「编译」的命令行工具，允许在**同一个脚本文件**里混合使用**不同的编程语言**，
各语言代码块**按顺序**执行，共同完成一个任务。

## 为什么叫 macaronic

- macaronic（形容词）本义「混用多种语言的」（源自马卡罗尼诗体 macaronic verse）。
- 本项目将其引申为新术语 "macaronic script"：一个混用多种语言代码、按顺序执行的脚本文件。
- 名实相符：文件本身就是"混语言"的。

## 核心价值

1. **多语言协同**：一块用 Python 处理数据，下一块用 Bash 调系统命令，
   再下一块用 Go 做并发——各取所长，一个文件搞定。
1. **自动衔接**：语言交界处（上一块结束 → 下一块开始）由工具自动处理数据传递，
   用户不用手工导出文件、写胶水代码。
1. **顺序流水线**：代码块即阶段（stage），天然构成流水线。

## 待确定（占位）

- 脚本中如何标注每段代码的语言（Markdown 块 / 注释指令 / 自定义 DSL）
- 跨语言衔接的数据格式（stdout→stdin 管道 / 共享 JSON state / 两者都要）
- 工具本身用什么语言实现（Python / Go / Node）

以上问题确认后再细化设计。
